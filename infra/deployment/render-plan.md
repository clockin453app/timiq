# Render Deployment Plan

Render deployment is tracked for the launch phase.

Planned services:

- Web service for `apps/web`
- Web service for `apps/api`
- Managed PostgreSQL database
- Environment variables stored in Render dashboard

No production deployment files are finalized until Docker, database migrations, and health checks are complete.
