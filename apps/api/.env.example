TIMIQ_ENV=local
TIMIQ_APP_NAME=TimIQ API
TIMIQ_API_HOST=127.0.0.1
TIMIQ_API_PORT=8000

# Local private file storage root (onboarding, clock selfies, work progress, etc.).
# Must be persistent in production; never expose this folder publicly (serve files only via authenticated API routes).
TIMIQ_STORAGE_ROOT=C:/timiq-storage

DATABASE_URL=postgresql+psycopg://timiq_app:timiq_local_password_change_me@localhost:5432/timiq_local

CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000